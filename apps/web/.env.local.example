# Copy this file to .env.local (which is gitignored) if you need to override
# the default Hall Core URL. Only a loopback (127.0.0.1/localhost) address is
# officially supported by this local prototype — see
# docs/architecture/0005-minimal-web-interface.md, "Local URL configuration".
NEXT_PUBLIC_HALL_CORE_URL=http://127.0.0.1:4310
