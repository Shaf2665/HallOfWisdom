{
  "name": "@hall-of-wisdom/codex-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Coding-agent adapter that executes the locally installed, ChatGPT-authenticated Codex CLI non-interactively and translates its JSONL output into normalized Hall agent events.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.13.0 <23 || >=24.11.0 <25"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "test": "vitest run",
    "verify:package-entry": "node verify-package-entry.mjs"
  },
  "dependencies": {
    "@hall-of-wisdom/agent-adapter-sdk": "workspace:*",
    "@hall-of-wisdom/protocol": "workspace:*",
    "cross-spawn": "^7.0.3",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/cross-spawn": "^6.0.6",
    "@types/node": "^24.0.0"
  }
}
